import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Saved = () => {
    return (
        <View className="">
            <Text className="">saved  Screen</Text>
        </View>
    );
};

export default Saved;